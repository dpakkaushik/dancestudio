import { NotBuiltYet } from "@/features/shell/components/NotBuiltYet";

/** /memberships — the Memberships tile on an artist's Home (18 Sep 2026). The
 *  prototype's S_memberships (16846): class packs and plans your students buy,
 *  by sessions or by months, priced and valid for a period. A money screen, so
 *  it waits on the live Cashfree account like the other passes. */
export default function MembershipsPage() {
  return (
    <NotBuiltYet
      tool="Memberships"
      what="Class packs and plans your students buy — by sessions or by months, priced and valid for a period."
    />
  );
}
