import { NotBuiltYet } from "@/features/shell/components/NotBuiltYet";

/** /routines — the Routines tile on an artist's Home (18 Sep 2026). The
 *  prototype's S_choreos (17115): the choreographies you teach, each with its
 *  style, level and the classes it runs in. Not built; the tile says so honestly. */
export default function RoutinesPage() {
  return (
    <NotBuiltYet
      tool="routines"
      what="The choreographies you teach — each with its style, its level and the classes it runs in."
    />
  );
}
