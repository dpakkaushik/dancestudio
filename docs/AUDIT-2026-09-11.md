# End-to-end audit — 11 Sep 2026

An overnight pass over the whole app, asked for in these words: *"check end to end
flow and find any bugs and remove those… check if backend is good enough to handle
the load and database is keeping all the required records as a standard app of this
scale should be doing."*

Every claim below was **run**, not reasoned about. Where something is asserted
without a proof behind it, it says so.

**Status key** — ✅ fixed and verified · 🟡 fixed, waiting on a migration ·
📋 reported, not built (needs your decision)

> ### ✅ ALL FIVE MIGRATIONS WERE APPLIED ON 11 Sep 2026 — ⚠ A SIXTH IS WAITING (§7)
> **Every 🟡 below is now green.** The revenue bypass is closed —
> `rls-proof-tenant-columns` passes — and **all 28 proofs pass** after the
> policy rewrite. The money and communication desks read real money; a pin
> saves and stamps `location_set_at`.
>
> Three things broke on the way and **all three were the test harness, not the
> product** — worth reading, because two of them are the kind of mistake that
> makes a correct fix look like a disaster:
> 1. `rls-proof-tenant-columns` judged by the rows PostgREST echoed back, and
>    **`@($null).Count` is 1, not 0** in PowerShell — so writes that were
>    correctly *refused* were reported as "WROTE", and the fix read as seven
>    failures. It now reads the column from the database before and after.
> 2. `rls-proof-discovery` unlisted a studio by PATCHing `tenants` **as the
>    owner** — it was setting itself up *through the hole being closed*.
> 3. `rls-proof-settings-screens` asserted an owner could PATCH `tenants`
>    directly, and that refusing the tick always *threw*. Both were the old
>    world; the refusal is silent now and the owner's door is
>    `set_tenant_location`.

---

## 1. Bugs found and fixed

### 🟡 A1 · An owner could put a business on Discover having paid nothing
**Severity: critical — it is a bypass of the app's only revenue mechanism.**

`tenants` carried one permissive RLS policy, `"owners update own tenants"`, that
named **no columns**. Every write the application makes to that table goes
through a `SECURITY DEFINER` function that validates first — but PostgREST is a
door too, and a direct `PATCH /rest/v1/tenants?id=eq.…` walked past all of them.

Proven end to end by `scripts/rls-proof-tenant-columns.ps1`:

```
6.  Owner flips studio -> artist page by hand: WROTE trainer_business  -- PAID GATE BYPASSABLE
6b. ...and then lists it, having bought no plan at all: WROTE listed   -- FREE PUBLIC BUSINESS
```

The two compose. `type` is a plain text column whose CHECK allows both values,
so a PATCH turns a studio (which costs a verified organization **and** its own
₹1,200/month subscription) into an artist page (which costs the ₹700 Artist
plan) and buys neither. And `guard_tenant_visibility` gates only
`new.type = 'studio'` — so once flipped, listing it is gated by nothing at all.

Note what *did* hold, because it says exactly where the line was: About's
220-character cap and the phone's shape were **refused**, because those rules
are CHECK constraints on the column. The rules that lived only inside a function
were not. **A rule the database does not keep is a rule that holds only at the
door you went in by.**

*Fix* — `supabase/migrations/20260913100000_doors_that_were_not_doors.sql`:
drops the orphan policy (nothing in the app updates `tenants` directly — verified
by grepping every `.from("tenants")` call site: all reads), adds a
`guard_tenant_type` trigger in the shape of the existing `guard_profile_role`,
so the gate cannot silently reopen if a policy is ever added back.

### 🟡 A2 · A `javascript:` URL could be stored as a business's link — and shown to the admin reviewing it
**Severity: high — stored XSS, aimed at the account that can approve verifications.**

The same hole, different column. `socials` was constrained to "an array of at
most twelve"; the rule that each entry is an `http(s)` address lived only in
`update_tenant_profile` / `update_my_profile`. A direct PATCH stored anything:

```
3. Owner stores a javascript: link: WROTE {url=javascript:alert(1); platform=Instagram}
```

The app renders links as `href={l.url}` in three places, and the third is the
serious one:

- `features/profiles/components/PublicProfile.tsx` — the public studio/artist page
- `features/profiles/components/PublicPersonPage.tsx` — the public person page
- `features/admin/components/VerificationQueue.tsx` — **the admin's verification queue**

A business applying to be verified could put a script URL in front of the one
account able to approve it. `public.profiles.socials` had the identical gap.

*Fix*, in two independent layers:
1. **The database keeps the rule now** — `socials_are_web_links()` as a CHECK on
   both columns, using the same regex the two functions already apply, so nothing
   valid becomes invalid. Any link already stored that is not `http(s)` is
   removed by the migration (it is a live payload, not data worth keeping) and
   the count is announced.
2. ✅ **`safeHref()`** (`lib/constants/socials.ts`) at all three render sites —
   an address that is not `http(s)` is not drawn. Two guards, because the cost of
   the second is a function call and the cost of being wrong is a script running
   in someone else's session.

### ✅ A3 · A proof script had been dead, silently, for weeks
`scripts/rls-proof.ps1` — the Step 1 profiles proof — did not run at all. It
threw a **parse error**, so it was never proving anything:

```
Unexpected token 'RLS' in expression or statement.
```

Cause: the file is UTF-8 without a BOM, and **Windows PowerShell 5.1 decodes a
BOM-less `.ps1` as ANSI**. A UTF-8 em dash (`E2 80 94`) then reads as `â€"` —
and `0x94` is U+201D, a smart quote **PowerShell accepts as a string
delimiter**. The literal ended early and the file stopped parsing.

*Fix*: a UTF-8 BOM on every `.ps1` carrying a non-ASCII byte (9 files). The
proof runs and passes now. Worth knowing generally: this will silently kill any
future script that picks up an em dash.

---

## 2. Will the backend take the load?

The architecture is sound — RLS everywhere, definer RPCs as the doors, a
provider-neutral money ledger, idempotent webhooks. What it did **not** have was
any of the indexing or policy shaping that decides whether that architecture
answers in milliseconds or in seconds. All of the below is in
`supabase/migrations/20260913090000_load_policies_and_indexes.sql`.

### 🟡 B1 · Every RLS policy was re-evaluated once per row
**63 policy bodies** called `auth.uid()` bare. Written that way it is a function
call attached to the row filter — executed for **every row the query considers**.
Wrapped in a scalar subquery, `(select auth.uid())`, the planner hoists it into
an InitPlan and runs it **once**. Same value, same decision. This is Supabase's
own documented fix and the single largest lever on RLS cost.

The migration rewrites them **out of the catalog** rather than by re-typing 63
policies — a re-typed policy is one that can differ from the one it replaced, and
a policy that differs is a security bug. It is idempotent, and it covers the nine
`storage.objects` policies this repo wrote (Supabase's own are left alone).

### 🟡 B2 · The search box was a sequential scan on every keystroke
`search_dance_os` matches names two ways: `like 'term%'` and `like '% term%'`.
The second **cannot use a btree index** — a leading wildcard has no prefix to
seek on. Every keystroke scanned `tenants`, `crews` and `events` end to end.

*Fix*: `pg_trgm` GIN indexes on the lowered name/title of tenants, crews, events,
classes and profiles — a trigram index serves both shapes.

### 🟡 B3 · Search called a definer function four times per candidate row
The events branch called `event_host_name(e.tenant_id)` **four times in the
WHERE clause of every candidate row** — and that function reads `tenants`, then
calls `event_host_is_public()`, which reads `tenant_members` and `profiles`.
One search across ten thousand events was forty thousand of those.

*Fix*: `public_host_ids(term)` resolves the matching public hosts **once** per
search, as a set of ids, and the branch tests membership. Same rows, same
visibility rule — a host that is not public is not in the set, so its name still
cannot be used to find anything.

### 🟡 B4 · Missing indexes on the columns the app actually filters by
Added: `tenants(city, type)` and a listed-only partial; `events(city, start_date)`
for published events; `classes(status, tenant_id)`; `profiles(role)` and
`profiles(created_at)`; `subscriptions(tenant_id)` and
`subscriptions(status, current_period_end)` for the renewal clock;
`payments(created_at)`, `payments(subscription_id)`, `orders(status, created_at)`,
`refunds(created_at)`; and a partial on `webhook_events` where `processed_at is
null`, which the admin dashboard counts on every load.

### ✅ B5 · Discover silently loses a city's classes past 200 published classes
`app/(app)/discover/page.tsx` called `findPublishedClasses(supabase, 200)` — the
200 most recently created published classes **across every city** — and then
filtered by city in JavaScript. The moment the platform has more than 200
published classes nationally, a city's classes start vanishing from Discover,
and the style rail's ordering goes wrong with them. Nothing warns; the list just
gets shorter. *(Fix below in §5.)*

### ✅ B6 · The auth server was on the critical path of every request
`proxy.ts` matched everything except `_next/static`, `_next/image` and
`favicon.ico`, and calls `supabase.auth.getUser()` — a network round trip to
Supabase Auth — on each one. For a signed-in user that was one auth call **per
request**, including every file in `public/` and **the Cashfree webhook route**,
where a slow auth server looks to Cashfree like a failed delivery and earns a
retry on the one path that records money. A hard scaling ceiling and a single
point of failure for all traffic. *(Standing item, NEXT TO DO #13.)*

*Fixed*: the matcher now also excludes static file extensions and
`/api/webhooks/*`. Nothing about who may see what changed — every page and
server action still resolves the user server-side, and a page that needs a
session still redirects without one. This only stops the app asking the auth
server about a PNG. No route is removed or moved, so Rule 14 does not apply.

### 📋 B7 · No rate limiting anywhere
There is none — not on search, not on `report_content`, `send_enquiry`,
`open_support_thread`, or sign-up. RLS decides *who* may do a thing, never *how
often*. For a public consumer app this is the main abuse surface.
**Recommended**: a `rate_limits` table plus one `rate_limit_hit(bucket, limit,
window)` definer function called from the server actions that front the risky
RPCs — additive, and it does not require touching any existing RPC body.
Not built: it wants your call on the limits.

### 📋 B8 · `nearby_tenants` has no pagination *(half done)*
It was hard-coded to 50 rows. The rewrite in
`20260913120000_a_business_has_a_place` makes that a parameter (default 50, max
200) — but there is still **no cursor**, so a city with more businesses than the
cap silently ends there and Discover can never show the next page. The limit
moving is not the fix; the cursor is.

---

## 3. Is the database keeping the records it should?

Yes, for the app as specified — 40 tables, every one with UUID PK, audit columns,
soft delete and RLS. The money ledger in particular is better than most: orders,
payments, refunds, payouts, payout lines, a `webhook_events` ledger giving
exactly-once delivery, and `subscriptions` as one row of truth with the provider
ids beside it.

What a marketplace at the scale you are describing normally also keeps, and this
one does not:

| Missing | Why it matters here |
|---|---|
| **Reviews / ratings** | There is no `reviews` table at all. For "the Zomato of dancers" this is not a nice-to-have — ratings are the ranking signal, the trust signal and the reason to come back. Nothing else in the schema can stand in for it. |
| **Search & impression events** | Nothing records what people searched, what they saw, or what they clicked. Without it there is no relevance ranking, no "popular near you", and no way to tell a studio why it gets no bookings. |
| **A delivery log for email/SMS** | Notifications are written to `notifications`, but nothing records what was actually *sent* and whether it landed. |
| **Device / push tokens** | No table, so no push notifications. |
| **Idempotency keys on mutating endpoints** | `webhook_events` covers the provider's side well. The user's side — a double-tapped Book button — relies on per-RPC logic instead. |

These are product decisions, not bugs, so nothing here was built. Reviews is the
one I would put first by a distance.

---

## 4. What passed, and what the harness itself got wrong

- **`npm run typecheck`** — clean. **`npm run lint`** — clean.
- **All 27 RLS proof scripts pass** (26 did; the 27th was the dead one in A3,
  and passes now). The 28th — `rls-proof-tenant-columns` — fails on purpose
  until §7's migration lands.
- **The admin panel and the location picker were verified with real sessions,
  not by reading the code**: `scripts/shots/shoot-admin.js` shoots all 13 admin
  screens through a throwaway admin and fails on any console error;
  `scripts/shots/shoot-location.js` drives the map and reads back the address.
  Both caught real bugs that review had missed — the wrong error code in the
  migration guard, and a React "setState during render".

### ✅ A harness bug: the e2e suite could never start its own server
`playwright.config.ts` had `webServer.command: "pnpm dev"`, and **neither known
machine has pnpm on PATH** — packages here go through `corepack pnpm@10`. It
stayed hidden because `reuseExistingServer` always found a hand-started dev
server first, so the command was never reached. The moment that server went away
mid-run, every remaining spec failed to boot one:

```
[WebServer] 'pnpm' is not recognized as an internal or external command
Error: Process from config.webServer was not able to start.
```

which reads exactly like eight broken specs. Now `npm run dev`.

### ⚠ The e2e numbers from tonight are not trustworthy, and it is the machine
Runs on this box are fighting for memory — Chrome alone is holding 24 processes,
free commit charge got down to **1.4 GB of 22.6 GB**, and the shell started
returning `fork: Resource temporarily unavailable`. The failures moved between
runs and every one of them was a timeout or an exhaustion error, never a wrong
value:

| Run | Failed | Error |
|---|---|---|
| full suite | `admin-moderation:313` | `net::ERR_INSUFFICIENT_RESOURCES` |
| full suite | `happy-path:252` | `toBeVisible` timeout on text that is rendered unconditionally |
| happy-path alone | `happy-path:1086` | navigation did not complete in 5 s |

`happy-path:252` passed on the next run; `:1086` had passed on the one before.
Both routes were then loaded by hand and compile fine.

**Settled once memory recovered (4.0 GB free). THE WHOLE SUITE IS GREEN:**

```
happy-path                          14 passed (3.5m)
the other seven specs               36 passed (2.7m)
                                 = 50/50, with every change below in place
```

None of the three failures above was a product bug. The rule the repo already
gives itself holds: **re-run on a quiet machine before believing a red suite.**
- The money paths read carefully: the Cashfree webhook verifies the raw-body
  HMAC before anything else, keeps an exactly-once ledger, and lands on RPCs
  that are idempotent again on the provider's payment id — so a redelivery,
  a crash between ledger and RPC, and a retry with a fresh timestamp are all
  safe. The two payload shapes are both read. I found nothing wrong with it.

---

---

## 5. The admin panel, in segments

Asked for: *"make it in segments where each segment opens a new page where admin
can see what is happening where. payment, subscription, approval request,
communication etc."*

Three of those four already had a page — subscriptions, approval requests
(verifications, with reports beside them) and half of communication (support).
Two things had none, and the Overview did not show the panel's shape at all.

### ✅ The Overview is a segment index
It led with figures and no map of the panel; the only way around was a nav row
that scrolls off the side of a phone. It now opens with **THE DESKS** — ten
cards, each naming a desk, what it is for in one line, and the number waiting on
it. Each opens its own page. The nav is reordered so the sections that carry
work an admin *owes somebody* come first, then money, then the registers, with
Audit last because it is read after the fact.

### 🟡 `/admin/payments` — the money desk (new)
The dashboard could say ₹40,000 had been captured and there was **nowhere to go
from the number**. An admin could not find one person's payment, could not see
the refund somebody had waited nine days for, and could not see what a studio
had paid its trainers. Three tabs on one page — **money in, money back, money
on** — because they are one question asked three ways.

- *Money in*: every payment, filterable, searchable by payer, email, business or
  provider payment id; each row says what it was for, split into **DanceOS's own
  revenue** and **studios' class income passing through**, and how much has since
  gone back.
- *Money back*: every refund, the ones somebody is still waiting on first, each
  with **how many days they have waited** — the number that says which to chase.
- *Money on*: what businesses have paid their trainers.

**Read-only, deliberately.** Settling a refund belongs to the studio whose class
it was — that is where the 48-hour window and the settlement rules live, and
moving that decision into the admin panel would quietly make DanceOS the
counterparty to every dispute. What an admin needs is to *see* it.

### 🟡 `/admin/communication` — what the platform says (new)
`notifications` is raised by triggers where the fact happens and read by one
person at a time, so nobody could answer *"did the platform actually tell
anyone?"* — the first question when a studio says nobody turned up, and the
first question after any change to a trigger. The desk shows volume, the
**share actually opened** broken down by kind (a kind with a long list and a
short bar is noise, and the first thing to cut), the conversations where the
last word is theirs, and the **last things the platform said, in its own
words**.

Outbound email and SMS are **not** shown, because the app sends none — the only
mail is Supabase's sign-in mail and nothing records a delivery. That gap is
listed in §3 rather than hidden behind a zero.

### ✅ A desk that says why it is empty
Both new desks read through RPCs that arrive with a migration. Before it is
applied they would have thrown a 500 — and *"you have not run the migration"*
and *"the app is broken"* look identical from a stack trace. They now draw a
notice naming the command instead.

Getting that right needed running it: the guard first checked Postgres's
`42883` (undefined_function) and **never fired**, because the call never reaches
Postgres — PostgREST resolves functions from its own **schema cache** and
answers `PGRST202`. Worth knowing beyond this screen: it is also what a
correctly-deployed function looks like for the few seconds before PostgREST
reloads.

---

## 6. The location picker

Asked for: *"right now app is missing location picker use any free maps api."*

**No account was needed for any of it** — OpenStreetMap tiles and Nominatim
geocoding, both keyless.

### The bug underneath the missing feature
`tenants` has had `lat` and `lng` since Step 5 and **nothing has ever written a
real one**. `create_tenant_with_owner` copies the **city centroid**; Step 5's own
comment said *"until precise address entry"*, and precise address entry never
came. So every studio in Pune sits on the same point — and
`nearby_tenants` measures centroid to centroid, which is **the same number for
every studio in the city**. Discover's "Studios near you", its distance chips
and its "nearest first" ordering have all been decorative. A 25 km radius
returns the whole city or none of it.

### ✅ What was built
- **`features/geo/components/MapPicker.tsx`** — a pan/zoom tile map over
  OpenStreetMap with a **fixed centre pin**: the map moves under the pin, the
  way every delivery app in India does it, because a marker you drag needs a
  finger on the thing it is being precise about and your own hand covers the
  answer. No mapping library — a slippy map is four Web-Mercator functions and a
  grid of images, and this repo's `node_modules` is a pnpm store that has
  already broken once on this machine. Drag, wheel, buttons and two-finger
  pinch; OSM attribution as the licence requires.
- **`features/geo/components/LocationPicker.tsx`** — three ways to arrive at a
  point, because people arrive differently: **type an address**, **use the
  phone's location**, or **just move the map**. City is a closed list in this
  app, so a detected city outside it is *reported, not written* — a studio
  silently moved to a city the app cannot show is worse than one whose city was
  not updated. ("Bangalore"/"Bengaluru" and friends are aliased.)
- **`/api/geocode`** — the only door the browser has onto a geocoder, and
  **signed-in only**: an open geocoding endpoint is somebody else's rate limit
  spent by strangers in this app's name. It carries the User-Agent Nominatim
  requires, queues to one request a second as its policy demands, and caches.
  An upstream failure answers empty rather than erroring — the geocoder being
  down must not stop anybody choosing a point.
- **`set_tenant_location`** — the owner-only door, which re-validates
  everything the server action already checked, refuses a point outside India,
  and stamps **`location_set_at`**: the column that lets anything downstream
  tell a point somebody *chose* from a centroid we defaulted to. Without it the
  two are indistinguishable.
- **"Near me" on Discover** — the chip swaps the city's centre for the
  browser's own point, so the distance on each card is a distance **to the
  person reading it**. Coordinates ride in the URL (the server does the search;
  the browser never touches the database) rounded to ~100 m.
- **Cards stop printing a distance that is not one.** Until a business has
  placed itself, its card says the place and stops. Every studio was previously
  showing the same "0 m" — centroid measured against itself.

### Verified, not assumed
`scripts/shots/shoot-location.js` makes a verified organization with a
subscribed studio, signs in as its owner, opens the sheet, drags the map, and
reads back what the pin landed on. Live result:

```
tiles rendered: 4
after the drag: THE PIN IS ON Kasba Peth, Pune, Pune City Subdistrict,
                Pune District, Maharashtra, 411001, India   Area: Kasba Peth
```

It also caught a real bug in the first version: reporting the pin from inside a
`setCentre(c => …)` updater called the parent's `setState` **during render**
("Cannot update a component while rendering a different component"). Fixed by
holding the centre in a ref for anything read outside a render.

### ⚠ One thing to know for later
OSM's public tile server and Nominatim both forbid heavy commercial use. That is
fine for a picker somebody opens once when setting up a studio; it is not fine
for consumer traffic at scale. The provider is behind **one environment
variable** — set `GEOCODER_PROVIDER=locationiq` (or `maptiler`) and
`GEOCODER_KEY` and the swap is a deploy, not a rewrite. Tiles would want the
same treatment at that point.

---

## 7. What is waiting on you

> **✅ The first five were applied on 11 Sep 2026. ⚠ One more is waiting** —
> `20260913140000_a_city_is_whatever_the_map_says`, the database half of the
> Google Maps / no-hardcoded-cities change (§9). The same command applies it:

```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/db-push.ps1
```

I could not run it — this session's auto-mode classifier blocks writes to the
production database, and routing around that would have been the wrong move. The
script reads the password from `.env.local` and percent-encodes it; `-DryRun`
lists what is pending without applying anything.

Until it lands, the app runs — the code falls back to the seeded
`city_centroids` table — but `set_tenant_location` still accepts only the
twelve seeded cities, aliases are not folded (Bangalore and Bengaluru are two
cities), a typed city has no centre, and the city chips are not "busiest first".
After it, re-run the 28 proofs and the e2e suite.

| Migration | What it does |
|---|---|
| `20260913090000_load_policies_and_indexes` | 63 RLS policies re-evaluated once per query; trigram + missing indexes; search stops calling a definer four times per row |
| `20260913100000_doors_that_were_not_doors` | **The revenue bypass and the script-URL hole** (§1) |
| `20260913110000_admin_money_and_communication` | The two new admin desks' reads |
| `20260913120000_a_business_has_a_place` | `set_tenant_location`, `location_set_at`, `nearby_tenants` gains `located` |
| `20260913130000_an_event_has_a_place` | `events.lat/lng`; `save_event` stores the venue's pin (§8) |
| ⚠ `20260913140000_a_city_is_whatever_the_map_says` | **NOT YET APPLIED.** `city_aliases` + `canonical_city()`, `remember_city()`, `set_tenant_location` accepts any city the map names, `discover_cities()` (§9) |

**Then run the proofs** — all 28 should be green, and two of them are the ones
that matter here:

```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/rls-proof-tenant-columns.ps1
```

That one **fails today, on purpose** — it is the proof of §1 A1/A2 and it is what
tells you the fix landed. Everything else is regression cover.

Order matters for one of them: apply `…doors_that_were_not_doors` before you
consider the platform safe, because the bypass it closes is live right now.

---

## 8. Follow-up, later the same night — your review, three asks

You looked at the screens and asked for three changes. All three are built,
verified with real sessions, and in this push.

### ✅ The admin panel as blocks, and no desk that is a scroll
> "the top sliding bar items doesn't what I want… give me boxes, block type
> views and an image/icon for each block… how am I gonna scroll down if there
> are 2k studios which applied? … at top should have a dashboard showing
> numbers — pending, approved etc — separate tabs for pending and approved,
> inside each tab a dashboard, filters, counts."

- **`/admin` is the nav, and ONLY the nav** — eleven icon blocks, each with what
  the desk is for in a line and the number waiting on it. Inside a desk there is
  only a slim "All desks ‹" line back; the pill bar is gone.
  *(Second pass, same day, on your follow-up — "the main screen will only have a
  designated block for every part of it": the figures that first sat UNDERNEATH
  the blocks are now a desk of their own, **Dashboard**, with its own block. The
  front door had been mostly reading material with the actual nav as a header
  above it, which pushed half the desks below the fold.)*
- **Every desk has one shape** (`desk-kit.tsx`): a hero, a **stat strip**,
  **tabs as blocks each with its own count**, a **search**, **one page of 25**,
  and a pager. All of it is URL state, so any view has an address.
- **Verifications** reads a real page from the database — `.range()` plus an
  exact `COUNT(*)` — under the admin policy that already exists, so 2,000
  applications is 80 pages, not one scroll. Tabs: **Pending** (oldest first —
  the person who has waited longest is at the top) · **Approved** (organizations
  *wearing* the tick, grandfathered ones included, with Revoke) · **Rejected**
  (with the reason they were given, and a way to approve after all) · **All
  orgs**. Search by name or city.
- **Support**: Waiting (the last word is theirs and no admin has read it) ·
  Open · Closed · All; search by account, subject or last message.
- **Reports**: exact counts per state; **Accounts** and **Businesses**: figures
  from the dashboard aggregate, tabs by kind and visibility.

*Caught by running it:* a value exported from a `"use client"` file is **not a
value on the server** — it arrives as a client reference, so `DESKS.map is not
a function` and three desks answered 500. Shared data now lives in plain
modules. `shoot-admin.js` shoots all 18 admin screens with zero console errors.

### ✅ Home: no "Verified organization" card after verification
> "after verification I don't need this verified organization box. user will
> create the studio and to make it discoverable he will subscribe then only
> studio will be visible."

The card renders only while the organization is unverified. Once the tick is on
the name, that *is* the verified state; the studios hub — with Subscribe beside
each studio — is the next step. The New-studio sheet's copy said "it goes on
Discover as soon as you create it", which was never true since 10 Sep; it now
says it stays private until subscribed.

### ✅ Events on the organization's Home
> "i told you events are gonna be at org level right now i cant see event on my
> org page. where it is?"

The desk has existed at `/business/{host}/events` since R15 — the only door to
it was a row inside the studios hub, two taps away, and Home never mentioned it.
**Studio Tools now has an Events tile** pointing at the organization's one
events host.

### ✅ The picker wherever an address is entered — and a map to check it on
> "still when i setup studio no location picker… wherever we are giving an
> address, city or location there should be a location picker as well… if can
> show it on map much better user will check nearest studio using location
> only."

Where it was: the business **Edit** sheet and a "Near me" chip on Discover.
Where it is now, in addition:

- **The New-studio sheet.** The pin rides in as `lat`/`lng` and is saved the
  moment the studio exists; typing an address fills Area and City from the
  map's own answer. A pin that fails to save never fails the studio — the toast
  says so and the same map is on Edit.
- **The event form.** The map link used to be typed by hand — a link nobody
  checks. The pin **writes it** (`maps.google.com/?q=lat,lng`) and fills the
  address; a fifth migration, `20260913130000_an_event_has_a_place`, stores
  the point itself (`save_event` reads it from the payload; a payload without
  it behaves exactly as before).
- **Discover as a map** (`🗺 Map` beside Near me): the shelf as pins, centred
  where the list is measured from, each pin a door to the business. Only
  businesses that have placed themselves are drawn — twenty pins on one point
  at the middle of town is not a map of anything — and the line under the map
  says how many have not.

`shoot-org.js` proves all three with a real verified organization: 10
assertions, zero console errors.

---

## 9. Google Maps, and the end of the hardcoded city list

> *"I have a Google Maps demo key, let's make it standard… remove the earlier
> maps setup you created, replace with the standard Google Maps approach
> everywhere… when selecting a city, or putting some address, instead of
> hardcoded city names I want to use APIs. Nothing should be hardcoded."*

### Does the demo key work? Yes — with limits worth knowing
I got this wrong at first: I assumed "demo key" meant a key copied from a code
sample, which never works. Google's **Maps Demo Key** is a real no-billing
feature, and `scripts/google-maps-proof.js` confirms it covers everything here:

```
ok   Places Autocomplete — 5 suggestions, first: Kothrud, Pune, Maharashtra, India
ok   Place Details       — Kothrud, Pune → 18.5074,73.8077 · city=Pune
ok   Reverse Geocoding (v4) — …Kothrud, Pune, Maharashtra 411038, India · city=Pune
```

**But Google states it is not for production**, and its daily quota *pauses*
rather than charges — so the map can simply stop one afternoon. Every screen
degrades to "the map is not available" and the form still works. A billed key is
two env vars and no code change.

### ✅ OpenStreetMap is gone
Deleted: the hand-rolled Web-Mercator tile map, the Nominatim client, and
`/api/geocode`. In their place: the **Maps JavaScript API** for the map,
**Places Autocomplete + Place Details** for search, and **Geocoding v4** for a
dragged pin. The search key stays on the server (it spends quota); only the map
key reaches the browser, where it must, protected by a referrer restriction.

### ✅ `DOS_CITIES` is deleted — and the city comes off the address
Twelve hardcoded names, wired into **14 files**, which meant a dance studio in
Kochi, Indore or Guwahati could not say where it was. My first replacement drew
the cities that already have a business as tap-chips with a search behind them;
you looked at it and saw a list — *"city, rather than picking from a hardcoded
list, should be picked from API"* — and you were right that a control which
needs explaining is the wrong control. So there are **no chips anywhere now**:

- **New studio** runs in the order you described: name → **Where it is**
  (Google suggests the address as you type, or one press of **◎ Use my
  location**) → **Area** and **City fill themselves from the Google address**,
  marked "from the address" → the map, to fine-tune → rooms. Typing in Area by
  hand stops the map overwriting it.
- **Events** do the same for the venue: the pin writes the address, the city
  and the Google Maps link.
- **Crews and enquiries** have no address to read from, so City is a Google
  city search. The typed name is always the last option, for the day the demo
  key's quota pauses — the database folds aliases either way.

`city_centroids` has stopped being a fixed list and become a **registry that
fills itself**: the first studio in a new city puts that city on the map. It is
read for two things only — Discover's city rail (cities that actually have a
business, busiest first) and where each form's map opens.

**The trap I had to design around:** Discover groups by *exact city string*. A
geocoder that says "Bengaluru" today and "Bangalore" for a place mapped by
someone else silently splits one city in two, and half its studios vanish from
the other half's listing. So every write folds through `canonical_city()`
against a `city_aliases` table seeded with the Indian renames that actually
collide — Bangalore/Bengaluru, Gurgaon/Gurugram, Bombay/Mumbai, and the rest.

### Two things that only running it could have found
1. **A demo key is refused for the *classic* Geocoding API** — and the refusal
   arrives as HTTP **200** with `REQUEST_DENIED` in the body, which reads like a
   dead key rather than the wrong API. **v4** works, on a different host, with
   the key as a header. It also answers in the same shape as Places, so one
   address parser serves both.
2. **The Maps script's `load` event does not mean Maps is ready.**
   `google.maps` is defined almost immediately — so every plausible readiness
   check passes and then `new google.maps.Map()` throws *"Map is not a
   constructor"*. `importLibrary` isn't the fix either; it doesn't exist on a
   plain script tag. The answer is Google's oldest mechanism: `callback=`.

Both are now commented at the point where someone would otherwise repeat them.

### Verified
`scripts/shots/shoot-location.js` drives the real picker end to end — Google's
own map container renders, typing returns 5 suggestions, choosing one moves the
pin, and the row afterwards reads
`lat=18.530263 lng=73.849859 area=Shivajinagar city=Pune location_set_at=…`.

`scripts/shots/shoot-new-studio.js` drives the New-studio sheet the way you
described it, and asserts it (18 checks): "Where it is" and "Use my location"
sit between the name and Area; with no pin the City is a search and there are
no chips; typing "Kothrud Pune" brings 5 Google suggestions; taking the first
fills `area="Kothrud" city="Pune"` and the pin, and City says "from the
address"; Change opens a Google city search — still no chips — and Keep leaves
it alone. It creates nothing and removes the one user it makes.

Also fixed while looking at its screenshot: the map read its zoom only when it
was built, so a searched address panned to the right point at CITY scale, and
"fine-tune the pin" at that scale is a two-kilometre gesture. It now zooms to
the street when a pin is placed.

### 🟡 And one the e2e suite caught right after: the map opening counted as a pin
Re-running the suite on the new sheet order, the events test failed: an event
published in **Pune** was on nobody's Discover. Its row said
`city = "Pipariya Khurd"`, `maps_url = …?q=22.9734,78.6569` — the centre of
India, and the village that happens to sit under it. Google's map fires `idle`
when it has *first rendered*, and the picker treated that as the owner placing
a pin: it reverse-geocoded the opening point and wrote the answer over the Pune
the owner had just chosen. A real studio owner who opened the sheet and never
touched the map would have been saved in Madhya Pradesh, with
`location_set_at` stamped as if they had chosen it.

The map now reports a settle only when its centre is somewhere the code did not
put it — not the opening point, not a search result's `panTo`, not a +/- zoom.
A drag or a pinch, which is what every map-pin app means by "put the pin on
your door". `shoot-new-studio.js` now waits for the map to render and asserts
that nothing was filled in.

### 🟡 And one more, from the very next run: one 502 took Discover down
With the pin fixed, the same events test failed again — this time the row was
right (`city = "Pune"`), and the dev log had the reason:

```
GET /discover?city=Pune&tab=events 500
classes.findPublished failed: <html><title>502 Bad Gateway</title>…cloudflare
```

Supabase's edge answered **one read in ~1,045** with a Cloudflare 502. Discover
reads the class list on every tab — on the Events tab only to sort the style
rail — and that one answer became a 500 page over a shelf of real, published
events. Two fixes, both the standard ones at any large consumer app's edge:

1. **Reads retry when the gateway blinks** — `lib/supabase/fetch.ts`, wired
   into every server client. GET/HEAD only (an RPC is a POST, and a studio
   created twice is two studios); 502/503/504 or no answer only (a 429 is
   never retried — that is how a hot path worsens its own rate limit); twice,
   at 300 ms then 900 ms; an aborted request stays aborted.
2. **Discover degrades instead of dying** — on any tab but Classes, a failed
   class read is logged and the style rail keeps its default order.

Two events published in Pune by the suite tonight are now the evidence for
both bugs; both are in the database, harmless, and will age out of the shelf.

### A third thing only running it could have found
The full e2e suite, run before the city migration was applied, emptied Discover.
`discover_cities()` did not exist yet, so `findDiscoverCities()` returned `[]`
— which I had written as a harmless degradation ("the chip is navigation, not
the page"). It is not navigation: that list is where Discover gets the **centre
it measures from**, so with nothing in it every radius search ran from the
middle of India and a city's own studios fell outside 25 km of it. It now falls
back to a plain read of `city_centroids`, which has existed since Step 5. The
rule, now in CLAUDE.md: a read that decides *where* a search happens cannot
degrade to nothing.

The same run found the **enquiry sheet had no city chips** — the prop was
there and nobody passed it down. A signed-in viewer's studio page now reads
the registry and hands it through `PublicProfile → EnquiryButton →
EnquirySheet`; a stranger's page does not, since their Enquiry is a link to
sign in. And `CityPicker` draws only eight chips, so a chosen city the
registry lists ninth was invisible — it is now brought to the front instead.

### What this leaves open
- ⚠ **Migration `20260913140000_a_city_is_whatever_the_map_says` is NOT yet
  applied.** Until it is: `set_tenant_location` still accepts only the twelve
  seeded cities, a city typed in the picker has no centre until an owner pins
  a business in it, aliases are not folded (Bangalore and Bengaluru are two
  cities), and the city chips read the seeded table rather than "busiest first".
  It is one command: `powershell -NoProfile -ExecutionPolicy Bypass -File
  scripts/db-push.ps1`.
- The Places/Geocoding cache in `lib/geo/places.ts` is in memory, per
  instance — fine for one dev server, per-instance on serverless.
